# Drop HMM regime CSVs here (per pair, e.g. USDJPY_regimes.csv).
# Auto-discovered by core.regimes.set_regime_folder, called from
# pages/eko_pricer.py with the current market data folder.
# Without these files the regime gates do nothing — they're optional.

